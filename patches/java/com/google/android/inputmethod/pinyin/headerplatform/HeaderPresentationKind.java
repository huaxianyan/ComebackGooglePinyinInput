package com.google.android.inputmethod.pinyin.headerplatform;

/** Renderer family selected by the Header platform, independent of module identity. */
public enum HeaderPresentationKind {
    NATIVE_CANDIDATE,
    REMOTE_SURFACE,
    NATIVE_ACTION
}
