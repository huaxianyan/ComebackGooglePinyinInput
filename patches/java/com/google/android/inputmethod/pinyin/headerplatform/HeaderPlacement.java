package com.google.android.inputmethod.pinyin.headerplatform;

/** A module declares desired placement; only the arbiter converts it into a render plan. */
public enum HeaderPlacement {
    EXCLUSIVE_CONTENT,
    CENTER_CONTENT,
    LEADING_ACTION,
    TRAILING_ACTION,
    PERSISTENT_ACTION
}
