# Rime 自动同步设计

> 状态：初版已构建，协议与静态检查通过。Pixel 10 Pro 上的隔离后台新增、删除、重新添加及关闭取消已验证，中断恢复尚未通过验收。本功能纳入当前版本，与手动同步一起验收。

## 已确认的产品行为

- 默认关闭，由用户主动开启
- 自动执行新增、删除和复活，开启时明确告知删除行为
- 默认间隔为 24 小时，复用自动备份的单选列表，提供 6 小时、12 小时、24 小时、2 天、3 天、7 天，以小时为存储单位
- 不再使用间隔滑块，旧滑块已存储的其他合法间隔继续显示，直到用户主动选择新值
- 不要求整点执行，实际运行时间由 Android 后台调度安排，可能延后
- 每轮只处理执行时所选目录中已有的快照
- 不检查文件是否「最新」，不检测 Syncthing 或其他设备的在线状态、同步进度
- 外部文件传播、下载、缓存和版本到达时机由相应软件负责
- 保留手动同步入口，复用现有同步目录、设备身份、基线与持久化复核
- 已确认仅保留于 Rime 的记录继续生效，不在每轮重新询问

自动同步的核心目标是开启后尽量无需用户维护。允许每次重新读取整份快照，不增加用户可见的「续传模式」或要求用户理解事务阶段。普通系统中断和临时失败由程序处理，不默认变成手动待办。

自动同步不是新增的网络服务，也不是目录监听功能。开启授权不等于允许自动清空词典、重置基线、忽略故障或扩大原生拒绝例外集合。

## 已核对的现有能力

### 自动备份不能直接作为周期调度器

`DictionaryAutoBackupCompat.request(context, false)` 只检查是否到期，执行机会来自输入法既有保存检查。它没有 JobScheduler、Alarm 或 WorkManager。长期不使用输入法时不产生备份，是该功能明确的设计行为。

自动同步需要后台执行机会，不能通过复制备份代码或在每次显示、收起键盘时触发来替代。自动备份保持原有行为，本次不顺便重做其调度。

### 同步执行能力已经存在

`RimeSyncSettingsCompat` 集中持有 Rime 单线程队列和互斥状态，并统一转换错误。`RimeSyncCoordinator` 已实现预览、执行、恢复和保留拒绝项的流程，原生访问继续由 `SaveDictionaryTask.sSaveLock` 保护。

自动入口应纳入同一个队列和错误处理路径，在同一次受控操作中复用协调器的规划、执行和完整校验。不得模拟点击 Compose，也不得重新实现文件解析、同步规划、原生写入或发布事务。

手动预览的删除确认仍保留。自动任务只有在开关已开启、开启告知已经确认时，才具有自动执行删除的授权。

## 调度方案

采用 Android `JobScheduler` 的普通周期任务：

- 使用唯一任务 ID，重复设置或恢复调度时更新同一任务
- 间隔直接来自持久配置，默认值、上下界和单位换算只在业务层定义一次
- 使用持久任务保留重启后的调度，复用已有 `RECEIVE_BOOT_COMPLETED` 权限
- 不增加精确闹钟权限、网络状态约束、充电前提或常驻前台服务
- 不引入 WorkManager 或借用旧 APK 的第三方后台同步链路
- JobScheduler 从 API 21 开始可用，低版本不得加载 JobService 类；设置入口沿用现有 Rime 功能的可用范围
- 系统负责安排周期内的运行时机，不把界面间隔描述为准点保证

依据 [JobInfo.Builder 官方文档](https://developer.android.com/reference/android/app/job/JobInfo.Builder)，周期任务执行时间具有弹性，持久任务也不等于准点承诺。真实执行仍受系统后台限制影响。

任务启动后只做资格检查和异步派发，SAF 与词典 I/O 不进入主线程。普通周期与临时失败后的重试共同管理同一个任务，失败后延后执行并退避，不制造短周期恢复循环，也不为每次重试增加用户提示。具体重试时机在实现中集中定义，不增加用户必填设置。

## 开关与异常边界

### 开启

需要目录配置完整、授权可用、已有成功同步记录，且没有待处理事务。开启时展示确认说明，例如：

> 开启后，应用会按所选间隔同步 Google 拼音与此目录中的词条，包括新增、删除和重新添加的词条。执行时机由系统安排。

取消确认时保持关闭。确认后保存开关并安排任务，不能仅改变界面开关而丢失调度。

### 与手动操作交错

自动任务与手动同步共用现有互斥入口，避免同一 Rime 事务并行执行。遇到已有操作时不抢占、不弹出确认框，也不创建第二份事务。自动备份和导入保留独立业务队列，仅共享既有 Native 锁。

### 失败与中断

- 系统回收进程、任务被停止或设备重启后，下次获得运行机会时自动检查实际状态并继续同步，允许重新读取整份快照
- 已有事务尚未结束时先按现有日志和幂等恢复规则收敛，不丢弃已提交的阶段，也不要求用户例行点击恢复
- 临时目录访问失败或本轮输入快照发生变化时保留可恢复状态，延后再试，不判断外部文件是否最新，不弹出每次失败的提示
- 区分暂时无法访问与持久授权失效，确实需要用户重新授权时才提示重新选择目录
- 新的原生拒绝或持久化复核失败仍执行既有安全校验，不盲目重复写入；无法安全自行处理时再请求用户介入
- 已确认的 Rime 保留记录正常沿用，不自动清除故障记录或重建基线，也不擅自扩大未经授权的拒绝例外集合
- 系统停止任务时不等待主线程，也不强行中断 Native 持久化临界段，不把被中断的运行记为成功
- 用户关闭开关后取消周期任务和待重试任务，已经进入事务的操作按既有安全边界结束或留下恢复记录，不承诺系统一定允许其运行至完成

只处理当前文件，不等于取消事务一致性校验。既有输入快照变化检查、Native 完整复核、Bridge 身份校验及文件发布协议继续保留，但不据文件修改时间猜测它是否为外部软件的最新版本。

## 最小实现范围

1. 在 Rime 业务层增加自动开关、间隔、暂停原因和调度协调，复用现有配置与结果边界
2. 增加单一 JobService 适配层及版本受控的 Manifest 注册，不新增同步执行器
3. 在现有 Compose Rime 区域增加自动开关、开启告知、间隔设置和暂停状态
4. 接通开启、关闭、间隔修改、应用更新及失败后的调度一致性
5. 保留手动同步和恢复行为，不要求重新选择当前目录或重建已有基线

状态页词条数误用 `getDictionarySize()` 的已知缺陷仍是发布前必修项，与自动同步分别处理，不把错误的 53 万显示当成压力测试规模。

## 验收范围

### 一条真实后台入口的端到端回归

在独立审计环境中完成开启告知和配置，由系统 JobService 入口执行生成快照的新增、删除与复活，确认事务完成、完整持久化及冷启动零变更。运行时不启动设置 Activity，也不要求先显示键盘。

若使用 `cmd jobscheduler run` 提前触发，证据应明确是系统入口强制运行，不宣称已经等满 24 小时或验证了真实周期时机。周期参数和重启后的调度保留分别核对。

### 必要的模块与边界验证

- 默认 24 小时及 6～168 小时边界，取消开启确认后仍关闭
- 同一任务的注册、更新与取消，重复配置不产生多份任务
- 手动操作占用时自动任务不并行修改词典
- 原生拒绝或复核失败后暂停，不绕过既有拒绝集合授权
- 已完成事务冷启动后可继续调度，正常系统中断留下的事务可自动收敛，不默认要求用户手动恢复

补充一次独立审计环境中的真实进程中断回归，确认下一次后台执行能自行收敛且冷启动预览仍为零。中断点应覆盖有明确数据风险的写入或发布阶段，不为所有代码行设计断点，更不拿用户正在使用的词库做破坏性实验。

上述失败边界分别来自已复现的原生拒绝、旧副本写入竞争和事务恢复要求，不扩充无依据的防御测试。

### 初版实现与构建证据

初版已接入独立的调度配置、唯一持久任务、JobService、启动及更新后的任务核对，以及现有 Compose 页面中的开关、开启告知和间隔控件。自动入口复用现有队列与协调器，开启条件由后端统一提供，未增加同步执行器或外部软件状态检查。

已完成的验证：

- Java 编译、75 个 Rime Smali 文件生成，以及 Rime 协议、状态存储、SAF、Native Bridge 和协调器测试
- 24 小时默认值、6～168 小时范围及区间外拒绝的模块测试
- 从原始 APK 完整构建，随后针对开启条件接口收敛和计数修复进行增量装配
- API 31、33、34、35、36 静态门禁，以及官方 Compose Material 3 运行时结构检查
- 6,633 个原始公开资源 ID 保持不变，Primary DEX 保持 `035`
- 隔离包身份、非 Debug 状态、SDK 声明、APK v1/v2/v3 签名与 16 KiB ZIP alignment 检查
- 最终 Primary DEX 中健康状态读取已使用 `getDictionaryCount()`，不再调用 `getDictionarySize()`

本地产物为 `2.0.10-rime-auto-sync-audit.1`，包名为 `com.google.android.inputmethod.pinyin.rimeautosyncaudit`，版本代码为 `4520423`。APK SHA-256：

```text
6fe81c25c5f98395743853bae4549d0fe29ffa64594591a6aa91797353d6509b
```

初版构建结束时尚未安装该产物，没有修改当前使用中的候选包、其词库或自动同步开关。以上是构建与静态证据，后续真机证据单独记录如下。

下一阶段仍须核对故障分类，验证临时失败能够重试、持续完整性故障不会无限重试，并完成真实后台入口、系统停止与进程中断恢复、开关取消及重新开启交错的隔离回归。计数修复也仍需空词库和非空词库的实际状态页验证。

### Pixel 隔离后台首轮回归

使用独立的 `rimeautosyncaudit` 包和本轮新建的 SAF 目录，在 Pixel 10 Pro / API 36 上从真实 Compose 页面选择目录并完成首次零变更手动同步。用户原先选中的 `rimenameaudit` 输入法保持不变。

已确认：

- 取消开启告知后没有注册系统任务，确认开启后任务进入 `waiting`
- 离开设置页后，通过 `cmd jobscheduler run -f` 触发真实 JobService，完成一条合成词条的新增、删除和重新添加
- 每轮发布后回到真实 Compose 手动预览，五项变更均为零
- 关闭开关后，系统查询确认对应任务已不存在
- 未操作正式包或当前候选包的词库，测试结束后默认输入法保持不变

上述任务是系统入口强制运行，不是等待真实周期的测试。重新添加的样本使用 `c=2` 覆盖此前的 `c=-1`。最初把 `c=-1` 改回 `c=1` 的样本没有覆盖删除标记，符合既有同幅值删除优先规则，不作为产品缺陷。

系统停止回归尚未通过：为了让任务保持可观察的运行时间，测试随后提供了共 1,001 条同一编码的合成记录，并执行 `cmd jobscheduler timeout`。系统确实报告停止了该 JobService，但这批样本混入了 Native 插入拒绝，不能用于证明正常中断后的自动收敛。

同签名诊断探针通过 `am instrument --no-restart` 读取隔离包的公开状态接口及线程状态，前后目标 PID 一致。观测值如下：

```text
busy=false
operationInProgress=false
phase=1
nativeFailureKind=6
nativeMissingCount=490
auto_enabled=false
auto_lastError=8
pending_jobs=0
```

`nativeFailureKind=6` 对应插入拒绝。程序保留待处理事务、关闭自动同步并取消任务，没有自动批准拒绝项，也没有继续盲目重试。这是拒绝后的安全暂停证据，不是后台线程卡死或中断恢复成功的证据，本轮也不据此推导 Native 容量上限。

当时界面仍显示进行中状态，与随后读取的后台状态不一致，刷新行为仍需定位。测试滚动期间间隔还变成了 39 小时，因此本轮不对间隔控件和系统调度参数的一致性作通过结论。后续应调整测试手势及样本编码分布，单独关闭这两个未验证边界。

诊断探针、独立审计包、本轮公共目录和每次生成的 UI dump 均已删除。正式包与当前候选包仍在，默认输入法未改变。真实进程中断、冷启动零变更、持续完整性故障处理，以及空词库和非空词库的实际计数显示仍待验证。

### 间隔列表与状态刷新回归

用户要求间隔设置沿用自动备份的列表选择逻辑，现已复用 `EnumeratedListSetting`，选项由业务层统一提供。对于旧滑块实际产生过的 39 小时，模块测试确认会保留当前值，不静默改变调度间隔。

已定位的刷新缺口有两处：`onResume()` 会因缓存的进行中状态而跳过读取，后台结果也没有向已打开的页面发送状态变化通知。现改为页面可见期间订阅业务状态变化，返回页面时重新读取，离开页面时解除订阅。只读状态查询不产生通知，避免读取触发再次读取，也不增加轮询或新的同步执行器。

隔离包 `2.0.10-rime-auto-list-audit.1`（版本代码 `4520424`）完成了以下 Pixel 真机回归：

- 真实 Compose 弹窗显示六个预设选项，取消选择后仍为 24 小时
- 选择 7 天后，系统 JobScheduler 参数也变为 7 天，随后可选择回 24 小时
- 页面保持前台时，合成快照的数据库名不匹配触发既有安全暂停，界面自动显示暂停状态，对应系统任务被取消
- 页面退到后台再返回后，仍正确显示暂停状态，无需重启进程或重新打开应用

本轮通过从原始 APK 开始的完整构建、Rime 模块测试、API 31/33/34/35/36 静态门禁、最终 APK 的 Compose 结构检查和 6,633 个原始资源 ID 核对。APK SHA-256：

```text
68730938f74a5d02e82f255eddb7f5c065aa929dab9563cfd00cbb79923f2bcb
```

本轮隔离包与公共临时目录已清理，用户原先选中的候选输入法保持不变。这次关闭了列表选择和暂停状态刷新问题，不替代尚待进行的真实进程中断、自动恢复或持续完整性故障验收。

### 发布条件

自动同步、状态页计数修复、既有手动同步与备份／导入共存，以及用户日常输入体验统一完成验收后，再决定合并和发布。本设计文档本身不代表自动同步已经实现或通过测试。
